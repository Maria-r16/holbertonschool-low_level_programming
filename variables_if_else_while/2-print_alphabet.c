#include <stdio.h>

/**
 *main - main function
 *Description: lowercase alphabetic sequence
 *Return: 0
 */

int main ()
{
	char alpha;

	for (alpha = 'a' ; alpha <= 'z' ; alpha++)
	{
	putchar (alpha);
	}
	return (0)
}
