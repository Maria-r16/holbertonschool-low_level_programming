#include <stdio.h>

/**
 * main - main function
 * Description:
 * Return: 0
 */

int main()
{
	int num;

	for (num = 0 ; <= num 9 ; num++)
	{
	printf (num);
	if (num == 9)
	{
	printf ('\n');
	}
	}
	return (0);
}
