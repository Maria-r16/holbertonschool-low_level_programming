#include "main.h"


/**
 * main - type the name followed by a new line.
 * @argc: number of arguments
 * @argv: pointer to an array of character strings
 * Return: 0
 */

int main(int argc, char **argv)
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
